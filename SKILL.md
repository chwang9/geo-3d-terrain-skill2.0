# Skill: 三维真实地形生成器 (Geo-3D-Terrain)　`v3.1.1`（2026-09-24）

> **一句话**：对 AI 说「生成 XX 的三维地形」→ 得到一个单文件 HTML，
> 真实 DEM 高程 + 真实卫星影像 + 中文类别地标标注，离线可看。
>
> **💬 你可以直接这样对我说（复制即可，不用记任何参数）**：
> - 「生成包河区的三维地形」
> - 「生成大蜀山的 3D 地形，要高清」
> - 「把这个 GeoJSON（行政区划 / 园区边界 / 航线）叠加成三维地形」
> - 「生成黄山市全市三维地形」（市域级大范围，自动分块纹理）
> - 「把刚才的地形再清晰一点 / 更立体一点 / 去掉地标 / 导出 GLB」
>
> 触发场景：用户要求"生成 XX 的三维地形 / 3D 地形 / 立体地形图 / 地形演示网页"，
> 或需要在真实地形上叠加地标做可视化展示时，
> 或**用户提供 GeoJSON（行政区划/园区边界/航线/点位）要在真实地形上叠加展示**时。
>
> **给 AI 的引导约定（v3.1.1）**：①用户问"能不能做 XX 地形"而未给地名时，主动用上面的句式
> 给出一条可直接发送的完整示例；②交付后主动告知后续可怎么说（如「再清晰一点」=重跑 `--clear`、
> 「更立体」=调 `--exaggeration`、「市域级」=`--sat-tiling 3`），让用户用自然语言就能迭代；
> ③用户输入含糊（只说"来一个三维地形"）时，默认挑一个知名地标地名直接生成，不要反问卡住流程。
>
> **v3.0.0 大范围完整方案**：分块纹理突破单张上限（自动 2×2，市域可显式 3×3）+ 网格同步增密
> （DEM 分辨率约 4×）+ 分块并行编码 + 高程下限分位截断。包河区全区实测：贴图 11 → **3.44 m/px**、
> DEM 80.6 → 20.1 m/px、影像编码 16.8 → 9.7s、缓存命中总耗时 12s。
>
> **v3.1.0 地标系统泛化**：地标不再假设目标是山峰——城区/平原同样能标出城市、交通枢纽、湖泊河流、
> 公园、医院高校等核心地标（OSM 标签→中文类别+图标，按类别配额轮询保均衡）；并修复"主峰"误判：
> 仅当 natural=peak/volcano 且有海拔/山名且靠近 DEM 最高格才叫主峰，平原区浮动标签改显示"最高点"。

## 能力边界

- ✅ 生成**真实**地形（真实 DEM 高程 + 真实卫星影像），不是程序化噪声
- ✅ 自动地理编码、**自适应地标系统**（山区标主峰、城区/平原标城市·交通枢纽·湖泊河流·公园·医院高校等核心地标，OSM 标签→中文类别+图标，按类别配额轮询保证均衡）、自动拉取 OSM 地标
- ✅ **v2.5 支持 GeoJSON**：点/线/面按真实高程"贴"到地形表面（面填充 + 描边丝带 + 点标记 + 名称标签），
  自动按包围盒定位、自动选 zoom；页面里还能**直接拖入 .geojson 即时叠加**
- ✅ 单文件交付，**Three.js 一并内嵌**，断网也能打开
- ✅ **v2.6 影像清晰度重做**：源影像自动取 z17（1.01 m/px）+ 纹理 2560 + JPEG 4:4:4 +
  两段 USM 锐化/局部对比去灰雾，同屏高频细节约为 v2.5 默认的 **2.1 倍**（实测）；
  `--clear` 把纹理拉到 4096（1.52 m/px）作极限档
- ✅ **v2.8 全面性能优化**：三级磁盘缓存（瓦片 / 地标 POI / 地理编码）+ Overpass 并发竞速去重试 +
  页面按需渲染，**同区域重跑 71.6 s → 2.1 s（34×）**，冷查询 Overpass 101 s → 16 s，
  页面静止时 0 次 GPU 绘制。产物体积与画质不变
- ✅ **v2.9 体积与工程化**：`--sat-format avif` 实测比 JPEG **小 23%**（5.99 → 4.67 MB）；
  `--dry-run` 先算「要下多少块 / 要多久 / 多大」再决定跑不跑；`--cache-prune` / `--cache-max-mb`
  给此前只增不减的缓存加上清理（v2.9.1 起 `--cache-prune` 不必再带地名）
- ✅ **v2.9 页面业务工具**：测距（水平/高差/坡度/直线）、任意两点高程剖面（含累计爬升）、
  **导出 GLB**（可进 Blender / Cesium / 数字孪生平台）、指北针 + 比例尺 + 光标经纬高
- ✅ **v2.9.1 正确性修复**：① 世界平面按米数走，**长条范围下垂直夸张不再各向异性失真**；
  ② 取点从"打满网格三角形"改**高度场步进**（实测 36.7 → 0.04 ms/次，**917×**）；
  ③ **DEM 坏点清洗**（实测把 -768 m 的孤立坏点修掉，否则色带/相对高差全被拉歪）；
  ④ 贴图分辨率与"是否已放大插值"改按范围**长边**算，纵向长范围不再误报
- ✅ **v3.0.0 大范围工程化**（v2.10 分块的完整化）：① `--sat-tiling` 支持 **3×3**（等效 12288px，
  市域级范围用，需 GPU maxTextureSize ≥ 12288，桌面独显一般 16384）；② **网格同步增密**：
  分块模式 `--max-size` 1024→2048 自动加倍，fit_zoom 随之抬 1 级，DEM 分辨率约 3–4×
  （包河区 80.6 → 20.1 m/px，顶点 151k → 2.4M 仍在 3M 预算内）；③ **分块增强/编码并行**
  （实测 4 块 AVIF 16.8 → 9.7s）；④ **高程下限按 0.1% 分位截断**：成片水体伪影
  （实测巢湖 -16 m、占 0.02%）不是孤立坏点、despike 修不到，会把色带与相对高差虚增四成；
  只截 min 侧（山峰是真实点状极值，max 侧不截）；⑤ dry-run 吞吐校准
  （实测 62 tile/s，旧值 6.6 高估 9 倍，改 30 保守值）
- ✅ **v3.1.0 地标系统泛化（修复"为山峰设计"的偏误）**：① Overpass 查询扩展到水域/水系/城市片区/
  交通枢纽/医院高校等，不再只查山峰+旅游+古迹；② OSM 标签→**中文类别+图标**（山峰🏔/湖泊🌊/城市🏙/
  车站🚉/高校🎓…），`desc` 不再暴露 raw OSM key；③ 按类别**配额轮询**选点（循环游标），避免某大类
  （地铁站/小池塘）刷屏挤掉核心地标，前 N 个优先类别保底出现；④ 小水体（pond/basin）降权为"水塘"💧；
  ⑤ **"主峰"判定收紧**：仅当 natural=peak/volcano 且有实测海拔（或山名关键字）且靠近 DEM 最高格才算主峰，
  过滤 OSM 上把观景台/风貌区误标 natural=peak 的节点（实测 包河区"草坡风貌区"曾被误当主峰）；
  ⑥ 信息面板"主峰海拔"→"最高海拔"，平原/城区浮动标签改"▲ 最高点 X m"（山区仍显示"▲ 主峰名 X m"）；
  ⑦ 修复旧去重 bug：原 `not peak_name` 分支会在"非山区无主峰"时把最高格附近所有地标全删掉，现仅在有真实
  主峰时去重同名近峰地标。包河区实测地标集：城市/城镇/合肥南站/高校/医院/政府/景点/古迹/湖泊/公园/河流/乡村等；
  大蜀山回归：主峰仍正确识别为"大蜀山"且去重正常
- ❌ 不做三维建筑、不支持倾斜摄影/实景三维模型

## 脚本清单

| 脚本 | 作用 |
|------|------|
| `scripts/prepare_terrain_data.py` | 主流程：地理编码（或 GeoJSON 定位）→ DEM → 影像 → 地标 → `terrain.json` →（可选）HTML |
| `scripts/generate_html.py` | 只做渲染：拿已有 `terrain.json` 重新出 HTML（改夸张系数时用它，不用重下瓦片） |
| `scripts/check_render.js` | 无头浏览器渲染校验 + 截图（**交付前必跑**） |
| `scripts/check_drop.js` | （可选）校验"拖入 .geojson 即时叠加"链路是否真的生效 |
| `scripts/check_tools.js` | （v2.9，可选）校验页面业务工具：测距文本、剖面 canvas、**导出 GLB 的魔数是否为 `glTF`**、HUD 数值、**取点耗时**（防回归到"打满网格三角形"） |
| （一次性脚本） | `bench_pick.js` 之类的取点/编码微基准放在工作区，不随技能分发 |

## 前置环境（首次使用必须检查）

Python 侧需要 `requests / numpy / Pillow / jinja2`。**不要污染用户全局环境**，用隔离 venv：

```bash
PY="C:/Users/wangch/.workbuddy/binaries/python/versions/3.13.12/python.exe"
VENV="C:/Users/wangch/.workbuddy/binaries/python/envs/geo3d/Scripts/python.exe"
"$PY" -m venv "C:/Users/wangch/.workbuddy/binaries/python/envs/geo3d" && \
"$VENV" -m pip install -q requests numpy Pillow jinja2
```

已装好则跳过安装，直接用 `$VENV` 执行脚本。

Node 侧（仅 Step 2b 校验需要，已具备则跳过）：

```bash
NODE_PATH="C:/Users/wangch/.workbuddy/binaries/node/workspace/node_modules"   # 已含 playwright-core
# 浏览器自动探测：本机 Chrome → Edge；找不到时交给 playwright-core 自己找
```

## 执行流程

### Step 1 — 生成（一条命令搞定）

```bash
cd <输出目录>
"$VENV" "<skill>/scripts/prepare_terrain_data.py" \
  --place "大蜀山" --zoom 14 --grid 3 \
  --out terrain.json --out-html 大蜀山三维地形.html
```

脚本会依次完成：地理编码 → 下载 DEM → 下载卫星影像 → 拉地标 → 写 JSON → 渲染 HTML。
全程有中文进度输出到 stderr，末尾打印**分阶段耗时**（`meta.timings` 里也有）：
`定位准备 / 下载 / 高程 / 影像 / 地标`，慢在哪一步一目了然。

**耗时（v2.8，实测 zoom14+grid3 大蜀山）**：

| 场景 | 耗时 | 说明 |
|---|---|---|
| 首次（无缓存） | 约 1.5–2 分钟 | z17 源 576 块瓦片 + Overpass 冷查询（已封顶 30 s） |
| 同区域重跑（缓存命中） | **约 2–3 秒** | v2.8 三级缓存：瓦片 + POI + 地理编码 |
| 极速档（`--sat-extra 2 --sat-max-size 2048 --jpeg-quality 84`） | 首跑约 25 秒、~4.1 MB | 清晰度随之退回 |

**别把 `--workers` 调太大**：实测 8→6.6、16→8.0 tile/s，24 并发被 Esri 限流反而掉到 1.8 tile/s（默认 12）。
**缓存想强制刷新**：加 `--no-cache`（同时禁用瓦片/POI/地理编码缓存），或删 `~/.cache/geo-3d-terrain` 下对应子目录。

**GeoJSON 模式（v2.5）**：传 `--geojson` 后定位方式完全改变——

```bash
# 行政区/园区边界/航线等：自动按包围盒定位 + 自动选 zoom + 图形叠加到地形
"$VENV" "<skill>/scripts/prepare_terrain_data.py" \
  --geojson 瑶海区.geojson --out t.json --out-html 瑶海区三维地形.html

# 多个文件合并；高德/腾讯导出的坐标要声明 gcj02
"$VENV" "<skill>/scripts/prepare_terrain_data.py" \
  --geojson 边界.geojson 航线.geojson --geojson-crs gcj02 \
  --place "某低空示范区" --out t.json --out-html 示范区.html
```

- 范围 = 所有要素的包围盒外扩 `--geojson-padding`（默认 6%），zoom 自动选
  （规则：原始 DEM 长边不超过 `--max-size`，避免下采样浪费瓦片；瓦片数不超过 `--max-dem-tiles`，默认 64）
- OSM 地标在 GeoJSON 模式下**默认关闭**（用户的图形才是主角），要就显式 `--landmarks 8`
- `--place` 在 GeoJSON 模式下只用作标题，可省（默认取面积最大要素的 name）
- 图形会按 DEM 分辨率自动 Douglas-Peucker 抽稀（`--no-geojson-simplify` 关闭；
  点数超 `--geojson-max-points`（默认 20 万）会自动加大容差，防止 HTML 爆体积）

**GeoJSON 支持的几何类型**：Polygon / MultiPolygon（含内洞）、LineString / MultiLineString、
Point / MultiPoint、GeometryCollection，可多文件混合。**坐标系**：默认 WGS84（GeoJSON 标准/GPS/OSM）；
高德、腾讯导出的一般是 GCJ-02，必须 `--geojson-crs gcj02`，否则图形会整体偏移约 500 m。

**属性里的样式（可选，逐要素生效）**：`name`/`NAME`/`名称`（标签文字）、
`color`/`stroke`（描边与点颜色）、`fill`（填充色，默认同描边）、`fillOpacity`（默认 0.22）、
`strokeWidth`/`weight`（线宽，默认 2）。颜色支持 `#rrggbb` / `#rgb` / `rgb(r,g,b)`。
统一强制配色用 `--geojson-color "#38bdf8"`，指定名称字段用 `--geojson-name-key`。

### Step 2 — 校验（必须做，别跳过）

**2a. 数据字段**（快，先跑）：

```bash
# HTML 是否真的生成、多大
ls -l 大蜀山三维地形.html
# 关键字段是否正确（高程范围应合理、山区 peak_name 不为空·平原/城区可空、has_sat 为 true；
# 影像侧重点看 sat_zoom / sat_native_m（源层级与原生分辨率）、sat_resolution_m（贴图分辨率）、
# sat_upscaled（true = 纹理细于源，纯插值，再调参无收益））
"$VENV" -c "import json;d=json.load(open('terrain.json',encoding='utf-8'));m=d['meta'];print({k:m.get(k) for k in ['place','min_elev','max_elev','peak_elev','peak_name','has_sat','size_w','resolution_m','sat_zoom','sat_native_m','sat_resolution_m','sat_upscaled','size_sat_w','size_sat_h']});print('landmarks',len(d['landmarks']));print('overlays',len(d.get('overlays',[])));print('timings',m.get('timings'))"
```

**2b. 真实渲染校验**（关键！JSON 正常 ≠ 页面能看。JS 一旦运行时报错，
页面会永远停在「正在构建真实地形…」转圈，而 `terrain.json` 一切正常）：

```bash
cd <输出目录>
NODE_PATH="C:/Users/wangch/.workbuddy/binaries/node/workspace/node_modules" \
"C:/Users/wangch/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" \
  "<skill>/scripts/check_render.js" "大蜀山三维地形.html" check.png
```

必须看到 `{"ok": true, "hasCanvas": true, "loadingHidden": true, "errors": []}`。
有叠加图形时还应看到 `ovLabelsTotal` 等于带 name 的图元数、`ovLabelsVisible > 0`。
`ok:false` 时看 `errors` 里的 `[pageerror]`，那才是真正的白屏原因。截图可肉眼复核。
脚本会自动起临时 HTTP 服务（file:// 下 blob 动态 import 可能被拦），无需自己开服务器。

**2c.（可选）拖拽叠加链路**：GeoJSON 相关改动后跑一次

```bash
NODE_PATH=".../node_modules" node "<skill>/scripts/check_drop.js" "输出.html" "某.geojson" drop.png
```

`ok:true` 表示拖入后叠加标签数量确实变化了（`before`/`after` 字段可对照）。

### Step 3 — 交付

用 `present_files` 打开该 HTML（会自动进入预览面板），并简述覆盖范围、高程、地标数量。

## 参数速查（prepare_terrain_data.py）

| 参数 | 默认 | 说明 |
|------|------|------|
| `--place` | — | 地名，中文/英文均可。与 `--lat/--lon`/`--geojson` 三选一（GeoJSON 模式下仅作标题） |
| `--lat` `--lon` | — | 直接给 WGS84 坐标，跳过地理编码（**最可靠的兜底**）；与 `--geojson` 同给时被忽略 |
| `--geojson` | — | **GeoJSON 文件路径（可给多个）**。按包围盒定位 + 自动选 zoom + 图形叠加到地形 |
| `--geojson-crs` | wgs84 | GeoJSON 坐标系：`gcj02` = 高德/腾讯导出（自动转 WGS84，否则整体偏 ~500 m） |
| `--geojson-padding` | 6.0 | 包围盒外扩百分比，给图形四周留点地形余量 |
| `--geojson-color` | — | 强制所有图形用同一颜色（如 `#38bdf8`）；默认读属性 `color`/`stroke` 或自动配色 |
| `--geojson-name-key` | — | 指定名称字段；默认自动试 `name/NAME/名称/title/label/id` |
| `--geojson-max-points` | 200000 | 图形坐标点总数上限，超出自动加大抽稀容差（防 HTML 爆体积） |
| `--no-geojson-simplify` | — | 不做几何抽稀（原样渲染，文件更大） |
| `--max-dem-tiles` | 64 | GeoJSON 模式下 DEM 瓦片数预算（自动选 zoom 用；越大越精细也越慢） |
| `--zoom` | 自动 | DEM 层级 3–15。地名模式默认 14；GeoJSON 模式默认自动（规则见上） |
| `--grid` | 3 | 地名模式的瓦片网格数 N；GeoJSON 模式下由包围盒决定，忽略此参数 |
| `--max-size` | 1024 | **网格**最大边长（像素），决定地形网格顶点数与几何平滑度（也影响 GeoJSON 自动选 zoom）；分块纹理模式下自动加倍到 2048（DEM zoom 抬 1 级、几何分辨率约 3–4×），显式给值则按给定值 |
| `--max-verts` | 3000000 | 网格顶点数硬上限（W×H）。超出则按最近邻抽稀网格（纹理仍保持 `--sat-max-size` 清晰度），专治 `--max-size` 调大时的顶点爆炸 |
| `--sat-max-size` | 自动 | **影像纹理**最大边长（像素），独立于网格。默认自动：`min(档位上限, max(2048, 源像素))`——即按源影像像素反推、小范围保底 2048，上限 standard 2560 / `--clear` 4096；显式给值则完全按给定值 |
| `--sat-extra` | 3 | 影像源层级下限 = DEM zoom + N（地名模式 zoom14 → **源 z17**；GeoJSON 模式随自动 zoom 平移）；受 `--max-sat-tiles` 预算约束，被压低时日志会提示。`--clear` 同样用 3——实测纹理 4096 时源 z18 只多 1.8% 细节却要 4 倍瓦片，要那 2% 才显式给 4 |
| `--sat-zoom` | 自动 | 钉死影像 zoom（不做瓦片预算收敛）；自动选层封顶 z18——实测 z19 与 z18 放大后无新细节，多下 4 倍瓦片纯浪费 |
| `--sat-zoom-max` | 18 | 自动选层的影像 zoom 上限 |
| `--sat-tiling` | auto | **大范围分块纹理**（v2.10，v3.0 扩展）：纹理切 N×N 块、HTML 端合成一张等效大纹理，突破单张上限。auto = 范围长边/档位上限 > 4 m/px（standard 约 >10 km、`--clear` 约 >16 km）自动开 2×2（等效 = 档位上限×N，--clear 时 2×2=8192）；给 `1` 强制单张（旧行为）；显式 `3` 供市域级（等效 12288px，需 GPU maxTextureSize ≥ 12288）。分块时源 zoom 自动抬升（瓦片预算 3600×(N-1)）、网格同步增密、未显式选格式时自动改 AVIF 省 36% 体积 |
| `--tiled-max-mb` | 24 | 分块纹理的影像体积预算（base64 后 MB），超预算按阶梯自动降质量（jpeg 最低 q72 / avif 最低 q52） |
| `--clear` | — | **最高清晰度模式**：纹理上限拉到 4096（1.52 m/px）+ 更强锐化/局部对比 + q86。实测约 **14 MB**、首跑约 3 分钟（是默认档 5.9 MB 的 2.4 倍）；只在需要放大细看时用。源层级仍为 z17（见 `--sat-extra` 说明） |
| `--max-sat-tiles` | 1200 | 影像瓦片数上限，超出自动降 zoom 控体积（被压低时日志会提示调大此值或钉 `--sat-zoom`） |
| `--sat-clarity` | 0.35 | 贴图局部对比「去灰雾」0–1（0=关）；`--clear` 时 0.5 |
| `--sat-saturation` | 1.06 | 贴图饱和度（1.0=不改，0=完全去色），轻微提饱和抵消压缩发灰。显式给 0 也会生效 |
| `--despike` | 100 | **高程坏点清洗阈值（米）**：与 3×3 邻域均值相差超过该值的孤立像素判为数据源异常值，用邻域均值顶上（`0`=关闭）。<br>实测每幅图常见 1–40 个 -700 m 级坏点，不修会把 min_elev 拉歪、压扁色带、虚增相对高差。<br>坏点占比超过 1% 时**自动放弃清洗**（那大概率是真实陡崖/海岸线，不是坏点） |
| `--sat-format` | jpeg | 影像编码（2560² 真实纹理实测）：`jpeg`=默认（3.44 MB / 230 ms，兼容最好）；`avif`=**2.20 MB / 618 ms，比 JPEG 小 36%**（产物 5.99 → 4.67 MB），需 Chrome85+/Safari16+ 才能解码，解不了会自动回退高程设色；`webp`=2.96 MB / 1337 ms，比 JPEG 小 14% 但编码最慢。AVIF 未显式给 `--jpeg-quality` 时按档位取 70 / `--clear` 72 |
| `--subsampling` | 444 | JPEG 色度抽样：`444`=默认（色度不降采样，彩色地物边缘更锐、体积略大）；`420`=更小但彩色边缘发糊 |
| `--jpeg-quality` | 85 | 影像质量（webp/jpeg 通用，70–95，越高越清晰、体积越大）；`--clear` 时 86 |
| `--sharpen` | 1.35 | 贴图锐化强度（1.0=不锐化，>1 更锐；v2.6 起为两段 USM：小半径提边缘 + 中半径提结构，带 threshold 防噪点放大）；`--clear` 时 1.8 |
| `--no-hillshade` | — | 不加立体光照；默认会把 DEM 微 hillshade 烤进贴图增强立体感 |
| `--hillshade-strength` | 0.5 | hillshade 强度（0–1，越大越立体；0 等于关闭） |
| `--hillshade-alt` | 45 | hillshade 光源高度角（度） |
| `--no-sat` / `--no-landmarks` | — | 跳过影像 / 跳过地标，用于快速或降级出图 |
| `--landmarks` | 自动 | 最多地标数：地名模式默认 16，GeoJSON 模式默认 0（显式给数即开启，给 0 关闭） |
| `--geocoder` | auto | `auto` = amap(需 AMAP_KEY) → nominatim → photon 依次兜底 |
| `--workers` | 12 | 并发下载线程数。**别超过 16**：实测 8→6.6、16→8.0、24→1.8 tile/s（Esri 限流）；v2.7 起默认 12 |
| `--out` | terrain.json | 中间 JSON 输出路径（`--out-html` 一步出图时不关心它，但改样式重渲染要用） |
| `--out-html` | — | 一步生成 HTML（不加则只出 JSON，需再跑 generate_html.py） |
| `--exaggeration` | auto | 垂直夸张系数，默认按"相对高差≈世界宽度18%"自动推荐 |
| `--three` | auto | Three.js 引入方式：`auto`=优先内嵌（离线可看）→失败回退 CDN；`cdn`=强制外链；`embed`=强制内嵌，下不到就报错 |
| `--cdn` | auto | 仅 `--three cdn` 时生效（jsdelivr → unpkg → esm.sh 择优） |
| `--no-cache` | — | 禁用**全部**磁盘缓存：瓦片 / 地标 POI(7 天) / 地理编码(30 天)，默认都开（`~/.cache/geo-3d-terrain`） |
| `--dry-run` | — | **只算计划不下瓦片**：打印层级、DEM/影像瓦片数（含命中缓存多少块）、预计耗时、预计产物体积后退出。大范围或 `--clear` 前先跑一眼，别盲目开跑 |
| `--cache-prune DAYS` | — | 只清缓存：删掉 DAYS 天前更新的文件后退出（`0`=全清）。**纯维护命令，不需要带 `--place`**（v2.9.1 修复：此前会被"必须给地名"的校验拦下）。清理范围仅瓦片/POI/地理编码，`three/` 工具链源码不动 |
| `--cache-max-mb` | 1024 | 缓存总盘上限，超出按**最旧优先**删除（每次生成收尾时检查）。缓存可再生，删了只是下次重下 |

单独重渲染 HTML（改夸张系数时很有用，不用重下瓦片）：

```bash
"$VENV" "<skill>/scripts/generate_html.py" --data terrain.json --out t.html --exaggeration 5
```

### 调参建议

- **GeoJSON 想更精细**：先加 `--max-size 2048 --max-verts 8000000`（网格变密、自动 zoom 随之抬高），
  再配合 `--clear`（纹理 4096）；只加 `--max-dem-tiles` 不加 `--max-size` 不会有分辨率收益
  （自动选 zoom 会让 raw 长边贴着 `--max-size`，避免被下采样浪费）。
- **GeoJSON 坐标整体偏了约 500 m**：九成是高德/腾讯导出的 GCJ-02，加 `--geojson-crs gcj02` 重跑。
- **GeoJSON 图形太密/HTML 太大**：这是抽稀在工作；`--no-geojson-simplify` 可关掉，或调低 `--geojson-max-points`。
- **用户说"影像不清晰"**：v2.6 默认已经是清晰档（源 z17 + 纹理 2560 + 4:4:4 + 两段 USM/局部对比，同屏高频细节约为旧默认的 2.1 倍）。
  还要更清晰就加 **`--clear`**（纹理 4096 → 1.52 m/px），代价：首跑约 3 分钟、文件约 14 MB（默认 5.9 MB）。
  注意清晰度的物理上限是**源影像原生分辨率**（Esri 在国内城区一般到 z18≈0.51 m/px，再高就是插值）——
  日志里「源 X m/px → 纹理 Y m/px」一眼能看出瓶颈在哪侧：源粗 → 加 `--clear`/`--max-sat-tiles`；纹理粗 → 加 `--sat-max-size`。
  纹理已细于源时会打 `[放大插值，细节已到顶]`（meta 里 `sat_upscaled: true`），此时再调参无收益。
  **反向陷阱（实测）**：当纹理才是瓶颈时（如 `--clear` 的 4096px=1.52 m/px vs 源 1.01 m/px），
  再把源抬到 z18 只多 1.8% 细节、却要 4 倍瓦片与 5 分钟——所以 `--clear` 默认不再加源层级。
- **想更快 / 更小**（不在乎那点清晰度）：`--sat-extra 2 --sat-max-size 2048 --jpeg-quality 84` →
  实测 **~4.1 MB**、首跑约 25 秒（缓存命中 17 秒），源退回 z16 + 纹理 2048。
  还想更小可再叠 `--subsampling 420 --sat-clarity 0`（彩色边缘会发糊）。反之追求极致用 `--clear`。
- **体积/速度实测（zoom14+grid3 大蜀山，v2.9 默认）**：单文件 **5.99 MB**，构成为
  影像 JPEG 3.44 MB（base64 内嵌后占 4.59 MB）+ Three.js 1.24 MB + OrbitControls 29 KB
  + GLB 导出器 78 KB + DEM 0.12 MB + 页面代码 0.04 MB（换 AVIF 后 **4.67 MB**）；
  首跑 1.5–2 分钟，**缓存命中约 2–3 秒**（v2.8：瓦片+POI+地理编码三级缓存）。
  `--clear`（纹理 4096）约 **14 MB**、首跑约 3 分钟；再加 `--sat-extra 4`（源 z18）约 14.4 MB、首跑 7.5 分钟，只换来 1.8% 细节。
  **4096 纹理性价比差**（字节翻倍只换 1.5× 线性分辨率，且屏幕上看不出来），所以只在 `--clear` 下放开。
- **换编码格式是最省事的减重（v2.9，2560² 真实纹理实测）**：JPEG q85 4:4:4 = 3.44 MB / 230 ms；
  WebP q85 = 2.96 MB / 1337 ms（小 14%，但编码最慢）；**AVIF q70 = 2.20 MB / 618 ms（小 36%，还比 WebP 快 2.2×）**。
  落到产物上：默认 HTML **5.99 MB → AVIF 4.67 MB（-22%）**，画质肉眼看不出差别。
  默认仍是 JPEG（兼容性最好）；AVIF 需 Chrome85+/Safari16+，**浏览器解不了会自动回退高程设色并提示**，不会白屏。
- **大范围（全区/全市）还是糊**：单文件单张纹理有物理上限（28 km ÷ 4096px ≈ 6.9 m/px），怎么调参都过不去。
  v2.10 起超过阈值会**自动 2×2 分块**（等效 8192px）拿双倍线性分辨率，v3.0 起**网格同步增密**
  （DEM 分辨率约 3–4×，几何不再是短板）；市域级（~50 km）可显式 `--sat-tiling 3`（等效 12288px，
  需桌面独显）。代价是体积约 4×（--clear + AVIF 实测全区 ~29 MB）与源瓦片数翻倍。
  嫌大就 `--sat-tiling 1` 回单张，或缩小范围（8×8 km 片区能到 1–2 m/px，才是真正的物理上限）。
- **导出 GLB 很大**（分块大范围下实测 ~214 MB）：贴图 4096 + 数百万顶点，进 Blender/Cesium 没问题，
  只是要等；要小就把 `--max-size` 调小再生成（网格顶点减少），或导出后在 Blender 里抽稀。
- **平原/湖区**（如合肥翡翠湖、长三角）：相对高差极小，自动夸张会顶到 10×，观感仍可能偏平，可手动 `--exaggeration` 再调；这类区域更推荐缩到 zoom 13–14 看整体地貌。
- **山地**：默认 10× 往往过头，用 `--exaggeration 2~3` 更接近真实比例。
- **想看更大范围**：降 `--zoom`（12 看 25 km、11 看 50 km）而不是狂加 `--grid`，否则瓦片数暴涨、体积失控。
- **网格顶点爆炸**：`--max-size` 调大（如 2048/4096）会让顶点数到数百万，老设备会卡。可设 `--max-verts`（默认 3M）自动抽稀网格——纹理尺寸独立（默认按源像素自动，`--sat-max-size` 可覆盖），清晰度不受影响。

## 页面交互（生成出来的 HTML 自带）

垂直夸张滑块（1–10×）· 重置视角 / 真实比例 · 影像贴图 ↔ 高程分层设色 · 网格线 ·
等高线（自动按高程范围生成约 12 条描线，卫星/设色两种模式都叠加）· 自动旋转（绕中心匀速转）·
导出 PNG（一键把当前视角存为图片）· 地标标注开关 · 信息面板折叠 ·
**图形叠加开关 + 图形名称开关（v2.5）· 把 .geojson 直接拖进页面即时叠加（坐标需落在当前范围内）** ·
左键旋转 / 滚轮缩放 / 右键平移 · 窄屏自适应。

**v2.9 业务工具**：`测距`（点两点 → 水平距离 / 高差 / 坡度 / 直线距离，结果贴在中点）、
`剖面`（点两点 → 底部浮层出高程剖面图，标注起终点高程、最高最低、**累计爬升**）、
`导出 GLB`（**真实比例 1×** 的地形 + 叠加层，贴图限 2048，可进 Blender / Cesium / 数字孪生平台；
导出前临时取消垂直夸张、导出后自动恢复）、`清除标记`；
底部 HUD 常驻**比例尺 + 光标经纬高**，右下**指北针**随相机方位转动。

叠加图形实现要点：**面** = ShapeUtils 三角化后逐顶点采样高程（支持内洞），**线** = 沿线按 DEM
分辨率加密采样后生成有宽度的"丝带"（LineBasicMaterial 的 linewidth 在多数平台被忽略，不能用 Line），
**点** = 小球标记。三者都把每顶点的经纬度存在 `mesh.userData.ll`，夸张系数一变就按新高度重算 y，
所以会实时贴合地形、不浮空不陷地。

## 失败处理（按优先级）

1. **地理编码失败**（三国源都无结果）
   → 先换个更通用的名字重试（"大蜀山 合肥"）；仍失败就直接问用户要经纬度，或用常识坐标，然后 `--lat/--lon` 重跑。**不要**因为失败就放弃。
2. **Overpass 地标为空**
   → 不是致命错误，地形照常生成，只是没地标。可缩小 `--grid` 或换 `--zoom` 重试一次。
3. **该区域无高程数据**（远海/无数据）
   → 换地点或降 zoom。
4. **网络整体超时**
   → 加 `--no-sat --no-landmarks` 只出高程，先保证有成果。
5. **打开 HTML 白屏 / 一直转圈**
   → **先跑 Step 2b 的 `check_render.js`**，看 `[pageerror]`，不要靠猜。
   两种已知原因：
   - JS 运行时错误 → 页面卡在加载动画。这类 bug 在浏览器控制台之外看不见，必须靠 2b 抓。
   - WebGL 不可用（沙箱/无硬件加速）→ 提示语会显示"当前浏览器未启用 WebGL"，换 Chrome/Edge 或开硬件加速。
   注意：**Three.js 默认已内嵌进 HTML，白屏基本与网络无关**；只有显式用了
   `--three cdn` 时才需要考虑 CDN 可达性（换 `--cdn unpkg`）。
6. **GeoJSON 图形没叠上 / 只有一半**
   → 先查 2a 里 `overlays` 数量；为 0 说明几何不完整（面 <3 点、线 <2 点会被丢弃）。
   图形位置整体偏移 ~500 m → 坐标系问题，加 `--geojson-crs gcj02`。
   图形超出地形边缘 → 加大 `--geojson-padding`（默认 6%，图形太贴边会被裁）。
7. **GeoJSON 特别大（省界/全国）导致 HTML 巨大或生成慢**
   → 靠自动抽稀 + `--geojson-max-points` 控制；实在不行先在 GIS 软件里简化再喂进来。

## 技术要点

- 数据源：DEM = AWS Terrarium（SRTM/GDEM，`elev = R*256 + G + B/256 - 32768`，z≤15）；影像 = Esri World Imagery（**原生分辨率一般到 z18≈0.51 m/px，z19 与 z18 放大后无差别**）；POI = OSM/Overpass
- **GeoJSON 叠加（v2.5）**：`prepare` 侧解析任意 Geometry → 按 DEM 分辨率 RDP 抽稀 → 拍平成
  `{kind: polygon|line|point, 坐标, name, color, fill, fillOpacity, width}` 列表写进 `terrain.json` 的 `overlays`；
  `generate` 侧按上节方式贴地渲染。GCJ-02 经 `gcj02_to_wgs84` 递归转换（保留第三维高程）。
- **自动选 zoom 的坑**：raw DEM 长边 > `--max-size` 时会被降采样 2 倍，多下的瓦片纯属浪费
  （zoom13 5×5=1280px ÷2 = 640，还不如 zoom12 3×3=768px，瓦片却多 2.8 倍），
  所以 `fit_zoom` 优先找"raw 长边 ≤ max_size"的最高 zoom，瓦片预算只作兜底。
  **影像侧同理**：v2.6 把纹理尺寸由「源像素」反推，就是为了避免出现"纹理比源还细"的假细节
  （那种情况下多花的字节全是插值，`sat_upscaled` 会标出来）。
- **变量名别撞车**：`build()` 里 DEM 的分数瓦片坐标 `fx0..fy1` 与影像的 `gx0..gy1` 必须分开存——
  影像计算会覆盖同名变量，导致 DEM 裁剪框错乱（v2.5 踩过，已注释警示）。
- **影像清晰度与网格解耦**（v2.2，v2.6 重做选层）：网格尺寸由 `--max-size` 决定（控顶点数），影像纹理由
  `--sat-max-size` 独立决定（控贴图清晰度）。v2.6 起**纹理尺寸默认自动**——按「源影像像素」反推并夹在
  [2048, 档位上限]，保证纹理不细于源（细了就是插值假细节）；源层级由 `--sat-extra` 下限 + `--max-sat-tiles`
  预算共同决定，自动封顶 z18（实测 z19 无新细节）。日志打印「源 X m/px → 纹理 Y m/px」与瓶颈提示。
- **纹理增强**（v2.6 重做，v2.3 起有雏形）：处理顺序 = LANCZOS 缩放 → 锐化/局部对比/饱和 → 乘 hillshade → 编码。
  ① `--sharpen` 为**两段 USM**（r1.1 提边缘 + r2.6 提结构，threshold 2/3 防平坦区噪点放大）；
  ② `--sat-clarity` 局部对比「去灰雾」（LAB 的 L 通道做高频回加，色度不动避免彩边）；
  ③ `--sat-saturation` 微提饱和抵消压缩发灰；
  ④ JPEG 默认 **4:4:4**（`--subsampling`，旧版 4:2:0 色度减半是彩色地物发糊的主因之一）；
  ⑤ `--sat-format webp` 保留但不默认：实测本项目纹理下 WebP 确实小约 10%，但编码慢一个量级（1.3 s vs 0.1 s），
     省下的不到 1 MB 不值得（method 4 vs 6：慢 2 倍只省 0.7%，故固定 method=4）。
  **历史坑（v2.6 前）**：旧版锐化用的是 PIL `ImageEnhance.Sharpness`，它本质是「原图与平滑图的线性混合」，
  对 2 m/px 级卫星影像几乎不产生清晰度——这是当时"影像不清晰"的隐性原因之一，现已换成真正的 USM。
  另外默认仍会把 DEM 重采样到纹理尺寸算出的微 hillshade 直接"烤"进贴图
  （`factor = (1-0.5k) + k·shade`，k=`--hillshade-strength` 默认 0.5），让平面影像也有立体感；
  hillshade 与 3D 材质光照叠加，故强度保持克制。
- **等高线叠加**（v2.4）：HTML 端由高程数组生成透明描线纹理（自动挑 10/20/50/100/200/500 m 的等高距，约 12 条），叠在地形上方（`polygonOffset` 避免 z-fighting）。卫星贴图模式和高程设色模式都生效，可用「等高线」按钮开关。线宽按 DEM 分辨率自适应约 1.6 像素。
- **DEM 异步解码**（v2.4）：JS 端高程解码默认放进 **Web Worker**（`OffscreenCanvas` + `createImageBitmap` 解 PNG，结果用 transferable ArrayBuffer 回传），不再在主线程 `getImageData` 阻塞——4096 级大网格也不会卡 UI。不支持/超时(8s)/报错时**自动回退主线程同步解码**，保证兼容。
- **DEM 与 SAT 并行下载**（v2.4）：先算好边界与影像 zoom（纯数学），再用两个线程池**并发**抓 DEM 与卫星瓦片；瓦片抓完后再串行做 hillshade/锐化/编码（依赖高程）。首跑省掉一半网络等待。
- **网格 LOD / 顶点上限**（v2.4）：`--max-verts`（默认 3M）硬上限。超出时按最近邻整块抽稀网格（`decimate`），纹理尺寸独立（默认按源像素自动），所以简化后贴图依旧清晰，几何开销可控。
- **编码/内存性能**（v2.7 微基准实测）：① DEM PNG 编码弃 `optimize=True`（逐行滤波搜索慢 8.8 倍）改 `compress_level=6`——高程数据体积只 +4%；② 影像马赛克在 numpy 阶段**先裁后转 Image**（z17 大范围省约 200 MB 的两次全图拷贝）；③ WebP 编码 method 6→4（快 2 倍、体积 +0.7%）。**试过但不划算的**：去掉 JPEG progressive（编码快 2 倍但文件 +6.9%）、LANCZOS `reducing_gap`（Pillow 12 默认已做整数预缩，无增益）——别再折腾这两处。
- **页面端**（v2.7）：WebGLRenderer 去掉 `preserveDrawingBuffer`（每帧 GPU 开销；「导出 PNG」本就在点击时先 `renderer.render()` 再 `toDataURL`，同任务内缓冲有效，无需常驻缓冲）。
- **三级磁盘缓存**（v2.8，重跑提速的绝对主力）：全部在 `~/.cache/geo-3d-terrain/` 下——
  ① `tiles/` 瓦片（DEM+影像，404 无数据带 24h TTL）；② `poi/` 地标（按 bbox+limit 命中，7 天）；
  ③ `geocode/` 地名→坐标（30 天）。实测 zoom14+grid3 大蜀山重跑 **71.6 s → 2.1 s**。
  想强制刷新用 `--no-cache` 或删对应子目录。**不要**给 Overpass 之类的慢查询加"每次必取最新"，
  这是缓存收益最大的一环。
- **Overpass 是最慢的一环**（v2.8 重点治理）：原实现用带 `Retry(total=3)` 的 Session 串行试三个端点，
  慢查询每次超时都要重试一遍——实测 30 s 的查询被放大成 **101 s**。修法三条：
  ① POI 专用 Session **不挂重试**（`max_retries=0`，已有三端点竞速兜底）；
  ② 三端点**并发竞速**，谁先成功用谁（串行的代价是慢端点累加）；
  ③ Overpass 语句 `[timeout:]` 从 60 降到 25、客户端超时 70→30。冷查询 **101.5 s → 16.2 s**。
- **分阶段计时**（v2.8）：`build()` 内 `mark()` 记录 `定位准备/下载/高程/影像/地标` 五段，
  末尾日志与 `meta.timings` 同时输出——排查"到底慢在下载还是 CPU"不用再猜。
- **影像编码兜底链**（v2.8）：webp → jpeg(按配置) → jpeg(4:2:0) → jpeg(不 optimize) → 低质量 jpeg。
  **必须逐级降级而不是同参数重试**：实测 Pillow 12 对部分大图做 4:4:4 编码会抛
  `Suspension not allowed here / broken data stream`，同参数重来必然再失败。
- **页面按需渲染**（v2.8）：`animate()` 里比较相机世界矩阵，没动 + 没拖滑块 + 没开自动旋转 + 无 dirty 标记
  就直接跳过 `renderer.render()`。实测**静止 3 秒 0 次 draw**（原先约 180 次），拖拽 1 秒 496 次——
  交互满帧不受影响，静止时省电、不烫、风扇不转。所有会改变画面的交互（开关图层/线框/等高线/地标/
  叠加图形/窗口 resize/拖入 geojson）都调 `markDirty()`，**新增交互记得补这一句**，否则改了不刷新。
- **标签更新去抖动**（v2.8）：`project()` 复用模块级临时向量与返回对象（不再每帧每标签 new）；
  `place()` 只在坐标真的变了才写 `style.left/top`（样式写入会触发重算，逐帧写纯属浪费）。
- **404 缓存 TTL**（v2.4）：无数据(404)瓦片仍缓存为 0 字节标记，但带 24h TTL；过期后重新请求，防止限流导致的瞬时 404 被记成永久无数据。网络异常不缓存。
- **坐标插值必须在墨卡托空间做**（像素行号与墨卡托 Y 线性相关，与纬度非线性）——v1 用纬度线性插值，低 zoom 时地标会偏几十米，v2 已修正
- 高德/腾讯返回 GCJ-02，脚本内已换算为 WGS84 再取瓦片
- Overpass 必须带 User-Agent，否则返回 406（v1 缺 UA 导致地标静默为空）
- 峰值先在平滑图上定位再取原始高程，避免噪点误判
- 输出是单文件 HTML，DEM / 影像 / **Three.js 全部内嵌**，真·离线可看。
  **v2.6 默认约 5.8 MB**，实测构成：影像 JPEG 3.3 MB（base64 内嵌后占 4.4 MB）＋ Three.js 1.24 MB
  ＋ OrbitControls 29 KB ＋ DEM 约 124 KB（1024² 网格，v2.7 起 compress_level=6 比原 119 KB 略大）＋ 页面代码/样式 38 KB；
  `--clear`（纹理 4096）约 14 MB。
  （注意 base64 会让二进制膨胀 4/3，看到"JPEG 3 MB 但文件 5 MB"是正常的。）
  内嵌实现：把 `three.module.js` 与 `OrbitControls.js` 放进 `<script type="text/plain">`，
  运行时包成 Blob URL 动态 `import`；OrbitControls 里的裸标识符 `from 'three'`
  需改写成 three 的 Blob URL。源码中的 `</script` 必须转义，否则会提前闭合节点。
- **不要在模块顶层（`init()` 之外）碰 `terrain` / `controls` 等异步创建的变量**——
  它们在 `await parseDEM()` 之后才存在。v2 曾在顶层写 `terrain.userData.map = ...`，
  导致每次打开都 `Cannot read properties of undefined` 白屏（v2.1 已修）
- **世界平面必须按米数走**（v2.9.1）：平面尺寸为 `WX = width_m·S`、`WZ = height_m·S`
  （`S = WORLD / max(width_m, height_m)`），**不是**恒为 WORLD×WORLD 的正方形。
  若把非正方形范围硬画成正方形平面，x 方向 1 世界单位 = `width_m/WORLD` 米、z 方向 = `height_m/WORLD` 米，
  而垂直方向固定是 `1/S` 米——三者只能对上两个，于是长边方向的坡被压平、短边的坡被拔高
  （实测 21×50 km 的长条范围，两个方向差 2.36 倍）。顺势把比例尺的米/世界单位统一成 `1/S`，才准。
  `check_render.js` 会断言「平面宽高比 == 范围宽高比」防回归。
- **取点用高度场步进，不要 raycast 地形网格**（v2.9.1）：地形是规则高度场，沿射线
  「AABB 裁剪 → 128 步粗进 → 22 次二分」即可定位交点；而 `Mesh.raycast` 没有 BVH，
  768² 网格就是 **118 万个三角形**逐个测，实测 **36.7 ms/次**，鼠标每动一次就掉帧
  （改后 **0.04 ms/次，917×**，命中结果完全一致）。`check_tools.js` 断言「每次取点 < 2 ms」防回归。
- **DEM 坏点必须清洗**（v2.9.1）：AWS Terrarium 偶发孤立异常像素（实测合肥周边一幅图里
  有 -768 / -696 / -532 m 这样的点，占 0.002%）。别看它少——`min_elev` 会被拉到 -768，
  相对高差虚增 60%，高程色带被压扁、自动垂直夸张变小、剖面 y 轴被拉爆。
  `despike()` 用「与邻域均值差 > `--despike`(默认 100 m)」判定，**逐轮放大邻域（3×3→5×5→7×7）迭代**，
  因为坏点常成簇出现、小邻域的均值本身也被污染；坏点占比 > 1% 时放弃清洗（那是真陡崖/海岸线，不是坏点）

## 更新日志

> 这里是**完整日志**（v1 起）；`README.md` 末尾有 v2.5 起的简明版，改版本时两处都要同步。

- **v3.1.1**（2026-09-24）**触发引导优化（纯文档/提示层，代码零改动）**。
  问题：技能未被选择时，WorkBuddy 把 SKILL.md 开头几行作为技能描述/输入提示展示给用户，
  旧开头只有抽象的"触发场景"，没有可复制的输入示例——用户不知道怎么打字才能触发。
  改法：① 把「💬 你可以直接这样对我说」的 **5 条可复制输入示例**提到文件最顶部
  （含地名/高清/GeoJSON/市域级/迭代调整五类说法）；② 新增「给 AI 的引导约定」：
  未给地名时主动给完整示例句、交付后主动告知后续自然语言说法（「再清晰一点」=`--clear`、
  「更立体」=`--exaggeration`、「市域级」=`--sat-tiling 3`）、输入含糊时默认挑知名地名直接生成不反问。
  README 同步新增「怎么对 AI 说」章节。

- **v3.1.0**（2026-09-24）**地标系统泛化（修复"为山峰设计"的偏误）**。
  根因：地标逻辑最初面向山峰场景，套到平原/城区（如 包河区）就出三类问题——
  (a) OSM 上把观景台/风貌区误标 `natural=peak` 的节点被当"主峰"（实测 包河区"草坡风貌区"被显示"▲ 主峰 57m"）；
  (b) Overpass 查询只含 peak/volcano/tourism/historic/worship/park，漏掉城区核心地标（巢湖、合肥南站等）；
  (c) 旧去重 `not peak_name` 分支会在无主峰的平原区把最高格附近所有地标全删。
  改法：① Overpass 扩展 `natural=water`/`waterway`/`place`/`aeroway=airport`/`railway=station`/
  `amenity=hospital|university|townhall` 等；② 新增 `_lm_category` 把 OSM 标签映射为**中文类别+图标**
  （desc 不再暴露 raw key）；③ 按类别**配额轮询**（循环游标）选点，避免地铁站/小池塘刷屏挤掉核心地标；
  ④ `water=pond/basin` 小水体降权为"水塘"💧；⑤ **主峰判定收紧**：仅 natural=peak/volcano 且有实测海拔
  （或山名关键字）且靠近 DEM 最高格才算主峰；⑥ 信息面板"主峰海拔"→"最高海拔"、平原区浮动标签改"▲ 最高点 X m"
  （山区仍"▲ 主峰名 X m"）；⑦ 去重仅在有真实主峰时去掉同名近峰地标。
  默认 `--landmarks` 地名模式 12→16。验证：包河区地标集变为 城市/城镇/合肥南站/高校/医院/政府/景点/古迹/
  湖泊/公园/河流/乡村 等均衡集合，peak_name 正确为空；大蜀山回归主峰仍正确识别为"大蜀山"且去重正常。
  放弃：按水域面积排序优选大湖（需 `out geom` 算面积，overfetch 代价大，改用配额+降权近似）。

- **v3.0.0**（2026-09-24）**大范围工程化：v2.10 分块的完整化 + 正确性修复**（工程级复审）。
  ① **网格同步增密**：分块模式下 `--max-size` 1024→2048 自动加倍，fit_zoom 随之抬 1 级
  （包河区 DEM z11→z13，56 瓦片仍在 64 预算内），DEM 分辨率 **80.6 → 20.1 m/px**，
  顶点 151k → 2.4M（3M 预算内）；图形抽稀容差随之变细（12.8 → 4.8 m）。
  ② **tiling 判定提前并简化**：从 sat 段（依赖源像素）移到定位阶段（decide_tiling 闭包，
  只看「范围长边/档位上限 > 4 m/px」），因为它决定 max_size、必须先于 zoom 选择；
  顺带修掉 sat 段残留的 `long_m` 引用（v2.10 遗留，并行编码改造时暴露）。
  ③ **`--sat-tiling` 扩展到 3×3**（等效 12288px，市域 ~50 km 用），瓦片预算按 N 放宽
  （3600×(N-1)）；HTML 端合成 canvas 受 maxTextureSize 保护不变。
  ④ **分块增强/编码并行化**（ThreadPoolExecutor，局部算子+只读共享输入）：
  实测 4 块 AVIF 16.8 → 9.7 s。
  ⑤ **高程下限按 0.1% 分位截断**（正确性）：实测包河区 z13 数据含巢湖成片水体伪影
  （-16 m 级、561 px、占 0.023%），despike 的孤立坏点判定修不到，min_elev 被拉到 -16、
  相对高差虚增 40%（53→74 m）；截断后 2–58 m 恢复正常。只截 min 侧——山峰是真实
  点状极值，max 侧截断会把主峰抹掉。
  ⑥ **GLB 导出贴图 2048→4096**：分块等效 8192 压到 2048 细节损失 4×；GLB 变大
  （实测大范围分块下 ~214 MB，可用）。
  ⑦ **dry-run 吞吐校准**：v2.10 实测 2520 块仅 40.8 s（≈62 tile/s，12 并发 keep-alive），
  旧值 6.6 tile/s 把耗时高估 9 倍；改 30（保守半值）。
  ⑧ 产物对照（包河区 --clear，缓存命中）：v2.10 57.6 s/27.8 MB → v3.0 **11.9 s**/28.9 MB
  （DEM 增密 +1.1 MB），DEM 80.6 → 20.1 m/px；大蜀山小范围回归与 v2.9 完全一致
  （6.0 MB、单张 2560、2.43 m/px，不触发分块与增密）。
  **放弃方案**：影像分块的 LOADED 按需流式加载（低 zoom 预览+高 zoom 流式）——与
  「单文件离线 HTML」定位冲突，维护成本高，明确不做；页面端 createImageBitmap 异步
  合成——Image 解码本身已在浏览器线程池，收益 <200 ms，不值得改。

- **v2.10**（2026-09-24）**大范围分块纹理：突破单张纹理上限**（回应"全区范围分辨率低"）。
  先测根因：单文件 HTML 单张纹理上限（standard 2560 / --clear 4096）除以范围长边就是贴图分辨率天花板
  （包河区 28.1 km ÷ 4096 = 6.87 m/px，再怎么调参都过不去）。改法：
  ① 新增 `--sat-tiling`（auto/1/2）：auto 在「范围长边/单张上限 > 4 m/px」时自动 2×2 分块，
  等效长边 = 档位上限×2（--clear 8192 / standard 5120）；② 源影像 zoom 自动向上抬到盖满等效纹理
  （瓦片预算放宽到 3600；显式 `--sat-zoom` 钉死时不动）；③ 分块时未显式选格式自动改 AVIF
  （体积约 4×，JPEG 会到 ~38 MB，AVIF ~24 MB）；④ 新增 `--tiled-max-mb`（默认 24）体积预算，
  超了按阶梯（jpeg 80/76/72、avif 64/58/52）自动降质量；⑤ HTML 端 `loadSatTexture` 支持分块：
  解码全部块 → 按 `META.sat_tiling` 网格 drawImage 到一张 canvas
  （受 `renderer.capabilities.maxTextureSize` 保护，老 GPU 等比缩小不白屏）→ CanvasTexture，
  地形网格/叠加/GLB 导出等其余管线零改动；⑥ prepare 侧逐块带 48px 余量做 USM/局部对比再裁边编码
  （局部算子+余量 → 跨块无缝；逐块编码还天然避开 Pillow 对超大图 4:4:4 的偶发失败），
  hillshade 整幅先算再逐块裁切，光照跨块连续；⑦ dry-run 同步分块计划（分块标注 + 按预算选档的体积估计）；
  ⑧ `check_render.js` 上报合成纹理尺寸 `texSize`。
  实测（包河区全区 22.6×28.1 km，--clear 自动分块）：贴图 6.87 → **3.44 m/px**（源 z16 2.03 m/px），
  HTML ~25 MB（AVIF），新增瓦片 ~2500 块冷跑约 6 分钟；小范围（大蜀山 6.2 km）不触发，行为与 v2.9.1 一致。

- **v2.9.1**（2026-09-23）**正确性复审与修复**（不改变 v2.9 的产物体积与功能）。
  ① **修复长条范围的各向异性失真**：世界平面此前恒为 WORLD×WORLD，非正方形范围下
  x/z 两个方向的「米/世界单位」与垂直方向的 `1/S` 对不齐，长边方向的坡被压平、短边被拔高；
  现改为 `WX=width_m·S` / `WZ=height_m·S`，比例尺的米/单位也随之统一为 `1/S`。
  ② **取点性能**：`pickAt` 由「raycast 地形网格」改为**高度场射线步进**（AABB 裁剪 + 128 步 + 二分），
  实测 **36.7 → 0.04 ms/次（917×）**，768² 网格下鼠标移动不再掉帧。
  ③ **新增高程坏点清洗** `--despike`（默认 100 m）：实测每幅图有 1–40 个 -700 m 级孤立坏点，
  会拉歪 `min_elev`、压扁色带、虚增相对高差；逐轮放大邻域迭代修正，占比 > 1% 时自动放弃（避免误伤真陡崖）。
  ④ 贴图分辨率与 `sat_upscaled` 改按范围**长边** `max(width_m,height_m)/max(sat_w,sat_h)` 计算——
  此前用 `width_m/sat_w`，纵向长范围会把"源更细、纹理还有余量"误报成"已放大插值"。
  ⑤ `--cache-prune` 不再要求 `--place`（此前这个纯维护命令会被"必须给地名"的校验拦下）。
  ⑥ `--sat-saturation 0`（完全去色）此前会被 `or 1.0` 悄悄改回 1.0，现已生效。
  ⑦ GeoJSON 包围盒被夹到极区时同步夹住分数边界，避免整行 NaN 被当最低值填成假的"深沟"。
  ⑧ `--dry-run` 的体积估算按短边实际比例折算，兜底字节数对齐 JPEG 实测值。
  ⑨ GLB 导出器只装配一次（不再每次点击都漏一串 Blob URL），未内嵌导出器时按钮直接禁用。
  ⑩ 两个校验脚本补防回归断言：`check_render` 验平面宽高比 == 范围宽高比，`check_tools` 验取点耗时 < 2 ms。

- **v2.9**（2026-09-23）**体积与工程化 + 页面业务工具**。
  **减重**：① 新增 `--sat-format avif`——实测（2560² 真实纹理）AVIF q70 = 2.20 MB / 618 ms，
  比 JPEG q85 4:4:4（3.44 MB / 230 ms）**小 36%**、比 WebP q85（2.96 MB / 1337 ms）**小 26% 且编码快 2.2×**，
  产物 **5.99 → 4.67 MB（-22%）**；浏览器解不了 AVIF 时自动回退高程设色并提示，不会白屏；
  ② 编码兜底链改为 avif → avif(降级质量) → webp → jpeg…，保证任何环境都能出图。
  **工程化**：③ `--dry-run` 只算计划不下瓦片（层级 / 瓦片数 / 已缓存多少块 / 预计耗时 / 预计体积，
  并给出换格式的体积对比）；④ 缓存此前只写不删（实测 4161 个文件 70 MB 且持续增长），
  新增 `--cache-prune DAYS`（0=全清）与 `--cache-max-mb`（默认 1024，超限时按最旧优先删），
  `three/` 工具链源码不参与清理。
  **页面工具**：⑤ **测距**（水平/高差/坡度/直线，结果贴在中点，随夸张系数贴地更新）；
  ⑥ **剖面**（两点高程剖面图 + 起终点/极值/**累计爬升**）；⑦ **导出 GLB**（真实比例 1× + 叠加层，
  贴图限 2048，实测 52 MB、魔数 `glTF`，可进 Blender/Cesium；导出前临时取消夸张、导出后自动恢复）；
  ⑧ 底部 HUD 常驻**比例尺 + 光标经纬高**，右下**指北针**随相机方位转动；
  ⑨ 新增 `scripts/check_tools.js` 自动校验上述工具（含 GLB 魔数）。
  **修了一个 v2.8 的回归**：⑩ 按需渲染只在「相机移动 / 有 dirty 标记」时才画，而首帧渲染时
  卫星纹理往往还没就绪，之后又没人触发重绘——打开页面看到的是**发暗的无贴图地形**
  （实测静止首帧画面平均亮度 22.6，随手拖一下相机才升到 45.4，肉眼很像"加载失败"）。
  现在改为**先把纹理 await 到位再建地形**，并暴露 `window.__geo3dReady` / `__geo3dTexReady`
  供 `check_render.js` 断言（等构建完成标记而不是死等固定秒数），同类问题以后能被自动拦住。

- **v2.8**（2026-09-23）**全面优化：文档↔代码对齐 + 代码质量 + 性能**（工程级复审）。
  **性能（实测，zoom14+grid3 大蜀山）**：① 新增**地标 POI 磁盘缓存**（7 天）+ **地理编码缓存**（30 天），
  与瓦片缓存组成三级缓存——同区域重跑 **71.6 s → 2.1 s（34×）**；② 治理 Overpass 这个最慢环节：
  专用 Session **不挂重试**（原 `Retry(total=3)` 把 30 s 查询放大成 101 s）+ **三端点并发竞速** +
  服务端 timeout 60→25、客户端 70→30——冷查询 **101.5 s → 16.2 s**；③ 页面**按需渲染**
  （静止 3 s **0 次 draw**，拖拽 1 s 496 次）+ 标签复用临时向量、坐标未变不写 DOM；
  ④ 日志末尾输出**分阶段耗时**（`meta.timings`），定位瓶颈不用猜。
  **代码质量**：⑤ 影像编码改成**逐级降级兜底链**（Pillow 12 对部分大图 4:4:4 编码会失败，
  原实现的"同参数重试"必然再失败）；⑥ 修过时注释（tex_cap 写 3072 实为 2560）、`encode_sat`
  文档串与实际行为不符（"优先 WebP"实为 opt-in）；⑦ `dem.min()/max()` 不再整表扫两遍；
  **文档**：⑧ 用 AST 从代码里抽出参数真值表逐条比对，修正 `--no-cache`/`--workers` 等描述，
  删掉未实测的 "12 并发 7.5 tile/s" 说法，耗时/缓存/性能章节全部改成实测数字。
  产物体积与画质与 v2.7 完全一致（HTML 5.9 MB）。
- **v2.7**（2026-09-23）**性能优化专项**（不提高分辨率，纯提速/省资源，微基准驱动）：① 默认并发
  `--workers` 8→12（实测 16 并发 8.0 tile/s 封顶、24 被 Esri 限流，12 是安全甜点，冷跑省 ~15%）；
  ② DEM PNG 编码弃 `optimize=True` 改 `compress_level=6`（**8.8× 提速**，体积 +4%）；③ 影像马赛克
  numpy 阶段**先裁后转 Image**（z17 大范围省约 200 MB 两次全图拷贝）；④ WebP method 6→4（2× 提速）；
  ⑤ 渲染端去掉 `preserveDrawingBuffer`（每帧 GPU 开销，导出 PNG 功能不受影响，已实测导出正常）；
  ⑥ geocode 去掉一次重复的 `Future.result()`。**明确不做**：去 JPEG progressive（快 96ms 但文件 +6.9%）、
  LANCZOS reducing_gap（Pillow 12 默认已整数预缩）。产物体积、清晰度与 v2.6 完全一致（HTML 5.9 MB）。
- **v2.6**（2026-09-23）**影像清晰度重做**（回应"卫星影像不清晰"）。先做客观测量再改：同一地面范围逐层拉
  Esri 影像归一化比高频能量，结论——大蜀山区域 z16→z17 高频细节 ×3（164→486），z17→z18 +12%（545），
  z18→z19 无新信息（549）。据此：① 源影像默认从 z16 提到 **z17**（`--sat-extra` 2→3，瓦片预算 576→1200），
  自动选层封顶 **z18**（`--sat-zoom-max`，z19 实测纯浪费）；② 纹理尺寸**默认自动**——按源像素反推、
  夹在 [2048, 2560]，不再无脑 2048；③ JPEG 默认 **4:4:4**（`--subsampling`，旧版 4:2:0 色度减半导致彩色边缘发糊）；
  ④ 锐化从 PIL `ImageEnhance.Sharpness`（实为原图与平滑图的线性混合，几乎无效）换成**两段 USM**
  （r1.1 提边缘 + r2.6 提结构，threshold 防噪点）+ **局部对比去灰雾**（`--sat-clarity`，LAB 亮度高频回加）
  + 微提饱和（`--sat-saturation`）。实测（大蜀山 6.2 km，同屏尺寸公平对比）：同屏高频能量 **2830 → 5977（2.1×）**，
  楼栋/道路/山纹明显更锐，无过锐白边。⑤ 新增 **`--clear` 最高清晰度档**：纹理 4096（1.52 m/px）+
  sharpen 1.8 / clarity 0.5 / q86（v2.8 实测 HTML 约 14 MB、首跑约 3 分钟）；⑥ 新增 `--sat-zoom`（钉死层级）与
  瓶颈诊断日志（`sat_upscaled` 时明确提示"细节已到顶"）。体积代价：默认 HTML 3.5 MB → ~5.8 MB，
  首跑 ~20 s → ~2 分钟（z17 需下 576 块瓦片，8 并发实测 6.6 tile/s，>16 并发被 Esri 限流反而更慢），缓存命中后 ~14 s。
  ⑦ **档位复审的额外发现**：`--clear` 最初把源也抬到 z18，实测发现那时**纹理（4096=1.52 m/px）才是瓶颈**，
  z18 源（0.51 m/px）只多 **1.8%** 高频细节（△Lap 10957 vs 10761）却要 **4 倍瓦片**（2304 vs 576）、多花 5 分钟，
  故 `--clear` 定稿为仍取 z17 源；想要那 2% 得显式 `--clear --sat-extra 4`。

- **v2.5**（2026-09-23）**GeoJSON 叠加支持**：① 新增 `--geojson`（可多文件），自动按包围盒定位、
  自动选 zoom（规则：raw DEM 长边 ≤ `--max-size`，避免下采样浪费瓦片）、DEM 按精确范围裁切；
  ② 点/线/面按真实高程贴地渲染——面 = ShapeUtils 三角化（含内洞）+ 逐顶点采样高程，线 = 沿线加密后生成
  有宽度的丝带（不能用 Line，linewidth 多数平台失效），点 = 小球标记；全部随垂直夸张滑块实时联动；
  ③ 支持从属性读样式（`color`/`stroke`/`fill`/`fillOpacity`/`strokeWidth`）与名称（`name`/`名称`/`title`…），
  `--geojson-color` 可强制统一配色，`--geojson-name-key` 指定名称字段；
  ④ 新增 `--geojson-crs gcj02`（高德/腾讯导出自动转 WGS84）、`--geojson-padding`、
  `--geojson-max-points`（超限自动加大抽稀容差）、`--no-geojson-simplify`、`--max-dem-tiles`；
  ⑤ GeoJSON 模式下 OSM 地标默认关闭（`--landmarks N` 显式开启），标题默认取面积最大要素的 name；
  ⑥ 页面新增「图形叠加」「图形名称」开关，并支持**把 .geojson 拖进页面即时叠加**（DragEvent → 解析 → 重建图层）；
  ⑦ 新增 `check_drop.js` 校验拖拽链路；`check_render.js` 增报 `.ovl` 标签数。
- **v2.4**（2026-09-22）性能与读图增强：① HTML 新增 **等高线叠加**（自动等高距、卫星/设色两模式通用、「等高线」按钮开关）；② DEM 解码默认挪到 **Web Worker 异步**执行（OffscreenCanvas 解 PNG，transferable 回传），4096 大网格不卡主线程，失败时回退同步解码；③ DEM 与卫星影像 **并行下载**（网络阶段并发，省首跑时间）；④ 新增 **`--max-verts`** 网格顶点硬上限 + 最近邻抽稀（LOD，防顶点爆炸，纹理仍清晰）；⑤ 404 无数据缓存加 **24h TTL**，防瞬时限流 404 被记成永久无数据。
- **v2.3**（2026-09-22）影像增强 + 交互：① 卫星纹理新增 **WebP** 编码选项（`--sat-format`，默认仍 JPEG——当时实测以为高熵纹理下 WebP 反而更大，
  **v2.8 复测更正：实际小约 10%**，只是编码慢一个量级，结论"不默认"不变）；② 纹理 `--sharpen` 轻微锐化；③ 默认把 **微 hillshade 烤进贴图**（`--no-hillshade` / `--hillshade-strength` / `--hillshade-alt` 可调），平面影像也有立体感；④ HTML 新增 **自动旋转** 与 **导出 PNG** 按钮（`preserveDrawingBuffer` 已开）。
- **v2.2**（2026-09-22）影像清晰度优化：新增 `--sat-max-size`（默认 2048，原 1024），**影像纹理与网格分辨率解耦**——纹理可独立精细到 ~1.5 m/px 而不增加网格顶点；脚本按目标纹理尺寸自动抬高影像 zoom；页面"贴图分辨率"改显示 `sat_resolution_m`；`--max-sat-tiles` 默认 64→576、`--jpeg-quality` 默认 82→85。
- **v2.1**（2026-09-21）修复必现白屏：模块顶层访问异步变量 `terrain` 的 JS 错误；
  Three.js 改为默认内嵌（`--three auto`），彻底摆脱 CDN 依赖；
  新增 `check_render.js` 无头渲染校验并把 Step 2 拆成 2a/2b。
- **v2** 墨卡托空间插值修正地标偏移、地标随夸张系数联动、地标开关生效、垂直夸张自动推荐、
  地形底座、高程分层设色、响应式布局。
- **v1** 首个可用版本。

## 不要做的事

- 不要用 `pip install` 装到用户全局 Python
- 不要在地理编码失败后直接告诉用户"做不到"而不尝试 `--lat/--lon`
- 不要把高德/腾讯导出的 GeoJSON 当 WGS84 直接用——必须 `--geojson-crs gcj02`，否则偏 ~500 m
- 不要用 `THREE.Line` + `linewidth` 画叠加边界——linewidth 在多数平台被忽略，必须用丝带（ribbonMesh）
- 不要谎报已完整生成——Step 2 的校验必须真的跑过
- 不要只跑 2a（JSON 字段）就交付，**2b 的渲染校验也必跑**；否则用户打开白屏才发现
